name: "workflow with params"

on:
  push:
    branches: "main"

jobs:
  test_variables:
    runs-on: "ubuntu-latest"
    env:
      VARIABLE: grenn    
    
    steps:
      - run: |
          echo "${{env.VARIABLE}}"

          echo "VARIABLE=gree >> $GITHUB_ENV"

      - run: "echo ${{env.VARIABLE}}"
      
